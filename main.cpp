#include <time.h>
#include <vector>

#include "Bender.h"
#include "AirBender.h"
#include "WaterBender.h"
#include "EarthBender.h"
#include "FireBender.h"

int main()
{
	srand(time(NULL));

	vector<Bender*> benders;
	bender.push_back(new AirBender("Aang", 100, 20, 20, 200, 10));

	int op = 1;
	while(op != 0)
	{
		cout << "
		switch(op)
		{
			case 
		}
	}

	return 0;
}

